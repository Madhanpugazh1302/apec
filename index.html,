<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login - Adhiparasakthi Engineering College</title>
  <style>
    body {
      background-color: #f4f4f4;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    .logo {
      width: 120px;
      height: auto;
      margin-bottom: 20px;
    }

    .login-box {
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      width: 300px;
      text-align: center;
    }

    .login-box h2 {
      margin-bottom: 20px;
      color: #b30000;
    }

    input[type="text"],
    input[type="password"] {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    button {
      background-color: #b30000;
      color: white;
      padding: 10px;
      border: none;
      border-radius: 5px;
      width: 100%;
      cursor: pointer;
    }

    button:hover {
      background-color: #990000;
    }

    .tagline {
      margin-top: 10px;
      font-size: 13px;
      color: #555;
      font-style: italic;
    }
  </style>
</head>
<body>

  <!-- Replace 'your-logo.png' with the actual filename of your emblem -->
  <img src="your-logo.png" alt="Adhiparasakthi Engineering College Logo" class="logo" />

  <div class="login-box">
    <h2>Student Login</h2>
    <form>
      <input type="text" placeholder="Username" required />
      <input type="password" placeholder="Password" required />
      <button type="submit">Login</button>
    </form>
    <div class="tagline">Study • Spirituality • Service</div>
  </div>

</body>
</html>
